using System;

namespace ConsoleApp1
{
    internal class Program
    {
        static void Main(string[] args)
        {
            string name;
            bool isAlive;
            int coins;
            float playerDamage;
            long quantityOfMoney;
            byte age;
            char selectedLetter;
            ushort bonus;
            double jump;
            uint playerHealth;
        }
    }
}
